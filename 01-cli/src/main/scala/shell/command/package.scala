package shell

/**
  * Provides the base class [[command.CommandRunner]] for defining custom commands. Contains
  * implementations of default commands: cat, echo, grep, pwd, wc.
  */
package object command {}